import type { BankingImageAsset } from "@/constants/assets";

export type SectionVectorDecorProps = {
  image: BankingImageAsset;
  wrapperClassName: string;
  imageClassName: string;
  sizes?: string;
};
